import { IBusiness } from "@/types/business";
import { View } from "react-native";

export default function Review({
  business,
}: {
  business: IBusiness;
}): React.ReactNode {
  return <View></View>;
}
